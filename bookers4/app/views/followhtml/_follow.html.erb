 <%= form_for(current_user.following_relationships.build(following_id: @user.id) ,url: relationships_path) do |f| %>
  <div><%= f.hidden_field :following_id %></div>
  <%= f.submit "Follow", class: "btn btn-large btn-primary follow-btn" %>
<% end %>