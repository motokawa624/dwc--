<%= form_for(current_user.following_relationships.find_by(following_id: @user.id), url: relationships_path, html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-large btn-danger follow-btn" %>
<% end %>