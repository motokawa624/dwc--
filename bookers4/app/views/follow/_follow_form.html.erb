 <% unless current_user?(@user) %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'followhtml/unfollow' %>
  <% else %>
    <%= render 'followhtml/follow' %>
  <% end %>
  </div>
<% end %>  